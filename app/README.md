# Application

The React + TypeScript + Vite application will be initialized here by the Repository Setup Agent after Bootstrap, Product Scope, and UI Fidelity are complete.

Do not initialize the app during Bootstrap.
